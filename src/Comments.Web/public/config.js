// Placeholder so `ng serve` and `ng build` have something to load. The container overwrites this
// file at start-up with the real environment values; see docker-entrypoint.sh.
window.__APP_CONFIG__ = {
  apiBaseUrl: '',
};
